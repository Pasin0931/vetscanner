# ============================================================================
# Vetscanner backend requirements
# Target: Python 3.9 (required for fastai==1.0.61 compatibility)
# ============================================================================

# --- Web framework -----------------------------------------------------------
fastapi==0.128.8
uvicorn==0.39.0
starlette==0.49.3
python-multipart==0.0.20

# --- Database ------------------------------------------------------------
sqlalchemy==2.0.51
psycopg2-binary==2.9.12

# --- Auth / sessions -------------------------------------------------------
authlib==1.6.12
httpx==0.28.1
itsdangerous==2.2.0
bcrypt==5.0.0
cryptography==49.0.0

# --- Environment / config -------------------------------------------------
python-dotenv==1.2.1

# --- AI / ML core ----------------------------------------------------------
# fastai v1 must be installed with --no-deps due to the unavailable
# 'pynvx' dependency on macOS:
#   pip install fastai==1.0.61 --no-deps
fastai==1.0.61
torch==1.13.1
torchvision==0.14.1
efficientnet_pytorch

# --- fastai v1's own dependencies (installed manually since --no-deps skips them) --
fastprogress==1.0.5
bottleneck==1.5.0
beautifulsoup4==4.15.0
matplotlib==3.9.4
numexpr==2.10.2
packaging==26.2
pandas==2.3.3
scipy==1.13.1
pyyaml==6.0.3
nvidia-ml-py3==7.352.0
numpy==1.26.4

# --- Whole slide image handling ---------------------------------------------
openslide-bin
openslide-python==1.4.2
opencv-python<4.10
shapely==2.0.7

# --- Report generation ----------------------------------------------------
reportlab==5.0.0

# --- Evaluation / metrics (used by evaluation_helper.py) ------------------
scikit-learn==1.6.1

# ============================================================================